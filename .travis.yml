sudo: required
dist: xenial
language: c

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq gcc-mingw-w64-x86-64 opencl-headers
  - ./scripts/travis_install_intel_opencl.sh

script:
  - make && ./crackalack_unit_tests
  - make clean; ./make_windows.sh
